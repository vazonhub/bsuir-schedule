/**
 * Master switch for the diary spotlight tutorial ("onboarding").
 *
 * Disabled in v3.2 — the onboarding never auto-starts and the "replay" entry in
 * Settings is hidden. The tutorial code is kept in place so it can be re-enabled
 * later by flipping this flag back to `true`.
 */
export const ONBOARDING_ENABLED = false;
