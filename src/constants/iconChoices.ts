/** Curated icon options for the exam icon slot. */
export const EXAM_ICON_CHOICES = [
  'school', 'book', 'document-text', 'clipboard',
  'pencil', 'create', 'alert-circle', 'warning',
  'flame', 'trophy', 'medal', 'ribbon',
] as const;

/** Curated icon options for the today/current-day icon slot. */
export const TODAY_ICON_CHOICES = [
  'time', 'today', 'calendar', 'calendar-outline',
  'sunny', 'star', 'arrow-down-circle', 'navigate',
  'location', 'flag', 'bookmark', 'heart',
] as const;

/** Curated icon options for the subgroup icon slot. */
export const SUBGROUP_ICON_CHOICES = [
  'person', 'people', 'person-circle', 'body',
  'school', 'reader', 'id-card', 'hand-left',
  'finger-print', 'eye', 'accessibility', 'happy',
] as const;

/** Curated icon options for the favorites/pin icon slot. */
export const FAVORITES_ICON_CHOICES = [
  'star', 'heart', 'bookmark', 'flag',
  'ribbon', 'diamond', 'flame', 'trophy',
  'medal', 'thumbs-up', 'pin', 'sparkles',
] as const;

/** Curated icon options for the location icon slot. */
export const LOCATION_ICON_CHOICES = [
  'location-outline', 'location', 'navigate', 'map',
  'compass', 'home', 'business', 'grid',
  'pin', 'flag', 'trail-sign', 'earth',
] as const;

/** Curated icon options for the clock/time icon slot. */
export const CLOCK_ICON_CHOICES = [
  'time-outline', 'time', 'alarm', 'timer',
  'hourglass', 'stopwatch', 'calendar-outline', 'today',
  'speedometer', 'pulse', 'trending-up', 'sunny',
] as const;

/** Curated icon options for the block/ban icon slot. */
export const BLOCK_ICON_CHOICES = [
  'ban', 'close-circle', 'remove-circle', 'eye-off',
  'volume-mute', 'notifications-off', 'trash', 'skull',
  'hand-left', 'stop-circle', 'alert-circle', 'shield',
] as const;
